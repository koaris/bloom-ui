{
  "name": "@koaris/tokens",
  "version": "1.0.4",
  "description": "Design tokens for the Koaris Technologies's bloom-ui design system. These tokens define the visual design primitives such as colors, typography, spacing, etc., providing a consistent style across the entire bloom-ui design system.",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm, cjs --dts",
    "dev": "tsup src/index.ts --format esm, cjs --dts --watch",
    "lint": "eslint src/**/*.ts* --fix"
  },
  "files": [
    "dist"
  ],
  "author": "guilhermesalviano",
  "homepage": "https://koaris.github.io/bloom-ui",
  "repository": {
    "type": "git",
    "url": "https://github.com/koaris/bloom-ui.git"
  },
  "keywords": [
    "tokens",
    "colors",
    "koaris"
  ],
  "license": "MIT",
  "devDependencies": {
    "@koaris/ts-config": "*",
    "@koaris/eslint-config": "*",
    "tsup": "^8.0.1"
  }
}
